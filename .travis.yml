os:
  - "linux"

before_install:
  - sudo apt-get update
  - sudo apt-get -y install make cmake xorg libc6-i386

addons:
  apt:
    update: true
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-9
      - cmake
env: COMPILER=g++-9

language:
  - "c++"

script:
  - mkdir build
  - cd build
  - cmake ..
  - make